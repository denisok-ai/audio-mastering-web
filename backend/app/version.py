# Версия приложения и дата сборки.
# Дата сборки подставляется при старте сервера (date.today()).
# Правило: увеличивать MINOR (0.X.0) при крупных изменениях продукта:
#   — редизайн модулей (админка, отчётность, настройки);
#   — новые значимые функции (защита LLM, пагинация, тестовые данные);
#   — смена контрактов API. PATCH (0.0.X) — только багфиксы и мелкие правки.
from datetime import date

__version__ = "0.4.0"
__build_date__ = date.today().isoformat()
